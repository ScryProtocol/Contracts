# x402s config template
# Copy: cp .env.example .env
# Or run: npm run scp:wizard

# ---------------------------------------------------------------------------
# Required keys
# ---------------------------------------------------------------------------
PAYEE_PRIVATE_KEY=0x...
HUB_PRIVATE_KEY=0x...
AGENT_PRIVATE_KEY=0x...

# ---------------------------------------------------------------------------
# Chain / contract defaults
# ---------------------------------------------------------------------------
# NETWORK=sepolia
# RPC_URL=https://ethereum-sepolia-rpc.publicnode.com
# CONTRACT_ADDRESS=0x07ECA6701062Db12eDD04bEa391eD226C95aaD4b
# (CREATE2 canonical address used by this repo flow across chains)

# ---------------------------------------------------------------------------
# Agent defaults
# ---------------------------------------------------------------------------
# NETWORKS=eip155:11155111
# AGENT_DEFAULT_ROUTE=hub
# AGENT_APPROVAL_MODE=auto
# AGENT_START_PAYEE=1
# Payment cap per request (smallest units of the offer asset: wei for ETH, 6 decimals for USDC).
# MAX_AMOUNT=1000000000000
# Hub fee cap per request (same smallest-unit convention as above).
# MAX_FEE=500000000
# Optional per-asset overrides (used before MAX_AMOUNT/MAX_FEE).
# MAX_AMOUNT_ETH=100000000000000
# MAX_FEE_ETH=500000000
# MAX_AMOUNT_USDC=500000
# MAX_FEE_USDC=50000
# MAX_AMOUNT_USDT=5000000
# MAX_FEE_USDT=50000
# AGENT_STATE_DIR=./node/scp-agent/state

# ---------------------------------------------------------------------------
# Offer config (recommended)
# ---------------------------------------------------------------------------
# Single source of truth for network offers + per-path prices.
# See offers.example.json for shape (offers + pathPrices).
OFFERS_FILE=./offers.example.json

# ---------------------------------------------------------------------------
# Hub storage / scaling
# ---------------------------------------------------------------------------
# JSON file storage (default)
STORE_PATH=./node/scp-hub/data/store.json
# Shared Redis storage (recommended for clustered mode)
# REDIS_URL=redis://127.0.0.1:6379
# Multi-worker hub mode (requires ALLOW_UNSAFE_CLUSTER=1)
HUB_WORKERS=0
# ALLOW_UNSAFE_CLUSTER=1

# ---------------------------------------------------------------------------
# Hub auth hardening (recommended)
# ---------------------------------------------------------------------------
# Optional bearer/x-scp-admin-token required for POST /v1/events/emit when set.
# HUB_ADMIN_TOKEN=change-me

# Max allowed skew for payee-signed admin requests
# (POST /v1/hub/open-payee-channel, /v1/hub/register-payee-channel, /v1/payee/settle)
# PAYEE_AUTH_MAX_SKEW_SEC=300
