# Music API

Canonical location moved to `node/scp-demo/music-api/`.

Run:

```bash
node node/scp-demo/music-api/server.js
```

Stream client:

```bash
node node/scp-demo/music-api/stream-client.js
```
